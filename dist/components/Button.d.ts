import { default as React } from 'react';

interface Props {
    children: React.ReactNode;
}
declare const Button: ({ children }: Props) => import("react/jsx-runtime").JSX.Element;
export default Button;
